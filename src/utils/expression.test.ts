import { beforeEach, describe, expect, it, vitest } from "vitest";
import { scanner } from "./expression";
describe('scan', () => {
    
  it("scan expression",async ()=>{
    const w = scanner("$('Test')")
    w.

  })
})
